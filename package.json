{
  "name": "chatify",
  "version": "1.0.0",
  "description": "🔐 Custom JWT Authentication",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install frontend && npm run build",
    "start": "npm run start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cloudinary": "^2.8.0"
  }
}
